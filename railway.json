{
    "build": {
      "builder": "nixpacks",
      "buildCommand": "cd apps/backend && npm install && npx prisma generate && cd ../frontend && npm install && npm run build"
    },
    "deploy": {
      "startCommand": "cd apps/backend && node index.js",
      "healthcheck": {
        "path": "/api",
        "interval": 10,
        "timeout": 5,
        "retries": 3
      }
    }
  }